# 线上配置文件
spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: 'jdbc:mysql://bj-cynosdbmysql-grp-m6g46hhs.sql.tencentcdb.com:24953/confidants?useSSL=false'

    username: root
    password: mysql56@.
  # session 失效时间
  session:
    timeout: 86400
server:
  address: 0.0.0.0
